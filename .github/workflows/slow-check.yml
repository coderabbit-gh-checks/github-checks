name: Fail PR Check

on:
  pull_request:

jobs:
  fail-check:
    runs-on: ubuntu-latest
    steps:
      - name: Always fail
        run: |
          echo "This check fails on purpose"
          exit 1
